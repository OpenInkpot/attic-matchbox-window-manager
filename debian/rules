#!/usr/bin/make -f
include /usr/share/cdbs/1/rules/debhelper-slind.mk
include /usr/share/cdbs/1/class/autotools.mk

DEB_CONFIGURE_EXTRA_FLAGS = --enable-startup-notification --enable-expat --disable-gconf
